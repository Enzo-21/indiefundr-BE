export const AUTOPILOT_INTER_PAYOUT_DELAY_SEC = 10;

/** Pause after a continuous autopilot batch before refresh / resume. */
export const AUTOPILOT_LOOP_DELAY_SEC = 30;

/** Max immediate re-runs when a workflow reports an interrupted (non-terminal) failure. */
export const AUTOPILOT_MAX_INTERRUPTED_RETRIES = 3;
